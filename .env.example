# MonBeat Frontend — Environment Variables
#
# WebSocket URL for the monbeat-server backend.
# Falls back to production Railway URL if not set.
NEXT_PUBLIC_WS_URL=wss://monbeat-backend-production.up.railway.app/ws
